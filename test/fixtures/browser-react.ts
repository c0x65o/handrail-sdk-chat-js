import * as React from "react";
import * as reactIntegration from "@handrail/chat/react";

export const hostReactVersion = React.version;
export const reactIntegrationExportNames = Object.keys(reactIntegration);
