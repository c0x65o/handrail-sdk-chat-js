# Historical validation scripts

These two hand-written scripts were tracked under `build/` in the original repository at `1f97255bcff4e0aab6c8071783aebdccb886380d`. They are preserved byte-for-byte for source completeness. They describe historical runtime and database setups; they are not current check commands. Current validation lives under `scripts/`, `test/`, and the example test directories.
