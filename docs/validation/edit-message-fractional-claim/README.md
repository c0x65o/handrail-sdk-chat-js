# Edit-message fractional-claim execution evidence

Fresh execution on 2026-09-05 for work request `003ca3b3-29b3-4d2e-a90e-f6d3d100795a`, Codex run `510bf914-16a0-49ae-a3d2-c8e79636d949`, owner goal `65b8f78e-01fc-4fe3-933d-f601ee4c4753`.

- TypeScript compilation: exit **0**, complete stdout/stderr in [compilation.log](compilation.log). Compilation used a source snapshot in an isolated temporary package; package build generators were not run because they would modify source/shared artifacts.
- Current implementation: exit **0**, **9 passed, 0 failed, 0 skipped**, including the fractional-millisecond conflict/expiry/replay/unchanged-domain-state regression. See [passing-test.log](passing-test.log).
- Old-assignment negative control: exit **1**, **6 passed, 3 failed, 0 skipped** (two failing subtests plus their parent). The fractional-claim subtest fails with PostgreSQL `23514`, `chat_idempotency_keys_timestamp_order_check`, at `completeIdempotency`. The ordinary stale-revision subtest also reproduces the defect. See [negative-control.log](negative-control.log). This is a constraint failure after successful backend setup, not a setup failure.
- Backend: project-owned managed dev PostgreSQL 16.13, resource `c699099d-668d-4a0f-a5a0-7f52923f3e32`, `127.0.0.1:34371/handrail_chat`. [Read-only MCP inspection](backend-provenance.json) verified project ownership, dev environment, healthy container and current binding. [Direct SQL probe](backend-probe.json) verified the worker connection. The worker DATABASE_URL was supplied to the harness as TEST_DATABASE_URL in child-process memory only; no credentials are included.

[commands.json](commands.json) records exact compiler/test argument arrays, working directories, timestamps, exit codes, and the redacted environment binding. The orchestration command was `node docs/validation/edit-message-fractional-claim/execute.mjs`, from the SDK repository. [execute.mjs](execute.mjs) retains the snapshot/compile/copy/mutation/probe/test logic and query instrumentation. Its derived-result parser was corrected after execution to anchor TAP `ok` lines so `not ok` is not mistaken for success; [test-results.json](test-results.json) was recomputed from the unchanged complete logs. No test output was rewritten.

[Source identity](source-identity.json) records Git HEAD, initial dirty status, actual input SHA-256 hashes (including both dirty source/test files), compiler/Node versions, and shared build hashes. [Compiled identity](compiled-identity.json) records both compiled trees. The negative tree differs only in `dist/server/edit-message-command.js`: `completed_at` and `updated_at` were restored from `GREATEST($3::timestamptz, created_at)` to `$3`. Tests were copied byte-for-byte. Both test processes assert and log package self-reference resolution into their own `dist` tree; negative failure stacks independently identify that tree. The preload only observes successful CREATE/DROP SCHEMA queries and does not replace database results.

[Preservation and cleanup](preservation-cleanup.json) verifies unchanged source/test inputs and shared `dist` contents. Each execution used the existing harness's unique `chat_edit_<uuid>` schema, recorded successful creation/drop, and a subsequent catalog query found its exact owned schema absent. No shared migrations, manual schema cleanup, runtime/config changes, commits, pushes, or deployments were performed. The linked Flutter repository was not touched and its final Git status was clean. Temporary compiled artifacts remain in the worker's own temporary directory for inspection; durable logs and hashes are retained here.

These are focused persistence checks, not a broader QA campaign. Work-request evidence is attached separately through the supported Handrail evidence tools; no owner-goal/queue state is changed by this repository work.
