# CI version-check ordering repair

The CI drift check now runs directly with Node before `npm ci`. Stale checked-out
generated bytes fail before any install lifecycle can rewrite them. Normal
`prepare`, build generation, compilation, and workflow controls are preserved.
This revision is ready for the existing independent reviewer; it is not overall
pilot acceptance or public Git consumer-install acceptance.

Task `24350c4f-985e-4762-ac15-445f69009f18`, strategy
`dcf3eef9-276a-4d9a-9552-b202ecacf701`, worker request
`4d2596f9-1c1a-4a50-a812-87850c324be3`, run
`2ee9d156-3ee3-484d-8d45-d7fceed0fe32`. The existing accountable lead and planner
retain ownership; this implementation does not provide independent acceptance.
No new controller or reviewer was created.

## Source reconciliation and exact artifact

JS HEAD remains `5dde4e190e14b867069e87fd7d5f47dddffde389`. All 12 source hashes
in the prior artifact matched at entry, including the pending generated-source
correction to 1.0.28. The checked-in version at HEAD still says 1.0.27; the
working generated source says 1.0.28 and was preserved byte-for-byte. Package
metadata and root lock remain 1.0.28. No index lock, merge, rebase or cherry-pick
was present. Deferred `main_workspace_in_use` synchronization was not bypassed;
no reset, stash, fetch, synchronization, staging or Git repair was needed for
these two nonoverlapping edits.

Only these implementation files changed in this run:

- `.github/workflows/node-tests.yml`: move the check before installation and use
  `node scripts/generate-package-version.mjs --check`, requiring only built-ins.
  Checkout, Node 22 setup/cache, main push/PR triggers, permissions, concurrency,
  timeout, ordinary `npm ci`, explicit build and Node test runner remain intact.
- `test/package-version-generation.test.mjs`: replace the inadequate check-vs-
  explicit-build index assertion with a behavioral lifecycle regression bound
  to the workflow's actual run-command order. Existing generation/version tests
  and their source checks remain.

`artifact.json` contains exact base-file hashes, entry working-file hashes,
current source hashes, and patch identities. Its 14-file source map includes
the prior 12-file target, the workflow and unchanged generator. Source digest:
`75d677f030511a3f7d5a61e13ace950b204407abcc350340afc7c7330193a7fe`.

- `repair.patch` is this run's two-file delta against HEAD, SHA256
  `bc02119a3706a88630ed108413f80d311ff17119cc2b20bae3980ca676038a64`.
- `publication-candidate.patch` also includes the pre-existing pending generated
  version correction, SHA256
  `cc19e714cb4344a62a5628ae96de43270351fe08f9a3262669f9e4e2642e9ff5`.

Both patches were applied to separate disposable reconstructions populated
from `git show BASE:path`; all resulting file hashes matched working source.
No repository was initialized and no final commit SHA is claimed.

## Behavioral verification

Exact commands, toolchain, outcomes and limits are in `commands.json`; stdout
and failure details are retained in both regression logs. Node v22.23.1,
npm 10.9.8; sequential execution, test concurrency 1 and inherited
`VITEST_MAX_WORKERS=1`. No unusual resource use was observed. The expected red
run recorded 108 MiB peak memory, zero swap and zero OOM kills.

From the SDK root:

```sh
node --test --test-concurrency=1 --test-name-pattern='CI version gate' test/package-version-generation.test.mjs
node --check test/package-version-generation.test.mjs
node scripts/generate-package-version.mjs --check
git diff --check
```

The selected regression was first run with the former workflow: exit 1, with
the stale case reporting `Missing expected rejection`. Actual `npm ci` ran
`prepare -> npm run build`, regenerated the value, and the later checker exited
0. This demonstrates that the added test detects the original CI defect.

After the workflow correction the same selected regression exited 0: all three
behavioral subtests pass (four TAP tests including the parent):

1. Stale 9.8.7-test generated source against 9.8.8-test metadata: checker exits
   1 with the expected drift diagnostic; generated bytes are unchanged and
   the preinstall/build lifecycle marker file does not exist.
2. Aligned source: checker exits 0, real `npm ci` invokes preinstall and normal
   prepare/build, then the explicit build runs. The marker records exactly
   `preinstall`, `build`, `build`; generated bytes stay aligned.
3. Former `npm ci -> npm run check:package-version -> npm run build` ordering:
   stale source is overwritten and every command exits 0, proving concealment.

The fixture copies the actual generator, uses the repository's prepare script
and leading build generation command, and executes extracted workflow run
commands in order with failure stopping later commands. Unexpected command
shapes fail the test. Each fixture has a matching, dependency-free lockfile,
private disposable cache, offline npm and explicitly enabled lifecycle scripts.
Only the unrelated TypeScript/CSS build tail is replaced with an observable
marker. Fixtures are deleted by test teardown. This is npm lifecycle evidence,
not SDK consumer installation or compilation acceptance.

Syntax, working-source version alignment and whitespace checks passed. No
TypeScript implementation changed; unchanged full builds, declaration checks,
PostgreSQL, Flutter and qualification suites were not rerun. Prior compilation
and stable-build evidence remains attributed to its producing runs.

## Preserved evidence and access limitation

`preservation.json` compares 1,655 existing tracked/evidence files: only the
two intended files changed; 1,653 are unchanged, including all 24 files in
`package-repair-20260911`. The Git index hash, both linked repo HEADs/status and
510 linked tracked-file hashes are unchanged. Consumer pins/locks, package
metadata, production @types/pg, generator and declaration regression are intact.

The prior verified target remains
`bc3dca8bfa8173359a8aff1a213fa6ee9cb584805daf1e6ad074572ba2173619`.
The saved brief/rules and all four frozen memories were read through scoped
MCP. `get_work_request` and `handrail_recent_run_history` confirmed independent
verifier `c36ebb72-dcef-4651-aa4e-da32dc368c47`, run
`695dd465-e3ba-4ee3-93a7-7ce33e9db604`, completed at 16:28:05Z. Its canonical
outcome reports passing production @types/pg, strict emitted declarations,
two-site TS7016 negative control, six tests and stable normal 1.0.28 builds.
That outcome and evidence references are saved in `prior-verifier-context.json`.

The complete worker record is not available through this worker's exposed
tools: `get_goal_completed_work_result` rejected the read because this run has
no owner_goal_id, no full worker-record reader is exposed, and the referenced
local validation-run directory is absent. Supplied complete-record SHA256
`8953c8f00ddf0c2a4059fe47816420dd591537d4eab2f8e6a2b537345c53ce2e`
is retained as a supplied identity, not independently rehashed here. Main's
record inspection and the canonical passing source outcome are preserved;
this limitation does not justify duplicate verification of unchanged findings.

## Publication and independent review handoff

The existing lead/planner should return this exact revision to the independent
reviewer and retain its outcome. Main owns any subsequent publication approval.
After any final metadata version bump, run normal generation/build and check
alignment; include final package metadata, matching root lock metadata and the
generated source in the same future commit, together with this reviewed CI
repair. Inspect the staged/committed bytes before any generating install can
conceal drift. Reconcile concurrent changes again at that boundary.

Public repository: `https://github.com/c0x65o/handrail-sdk-chat-js.git`.
After authorized publication, record the actual full 40-character commit SHA.
The independent reviewer must then install via public HTTPS Git pinned to that
actual SHA, retain a matching consumer lockfile, verify normal prepare/build,
strict installed declarations and package/generated/compiled identity, and
perform a locked clean reinstall (`npm ci`). SDK production @types/pg must
satisfy its own declaration dependency; do not add a consumer workaround.
No archive, file, workspace, registry, branch or tag SDK substitute qualifies.
Existing consumer pins remain held. Final commit SHA is null in this artifact.

No commits, pushes, publication, consumer pin/lock changes, services, database
connections, resource management, runtime configuration, deployment, adoption
or Handrail database/queue mutations were performed. Hitcents project
`0f617308-8c3a-4ad4-83e7-34b1fda024b3` remains read-only. Accepted integration
planning, historical inconclusive QA/media, fixture provenance, live/native
limitations and 4px Send-padding limitation remain in force. This source-only
ordering repair does not establish overall pilot readiness.
